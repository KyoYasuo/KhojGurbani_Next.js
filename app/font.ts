import { Inter, Tinos } from 'next/font/google'

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
})

export const tinos = Tinos({
    subsets: ['latin'],
    display: 'swap',
    weight: '400'
})